# Instructor: generate the receipts

Run `python3 dataset/.generate/build.py` to emit the four source files, the hidden ground truth, and `dataset/claim.reference.csv`. Do not hand-write receipts or back-fill answers.
